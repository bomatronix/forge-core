// InboundService has been moved to @forge-core/channels (libs/channels/src/inbound.service.ts).
// Re-export for any remaining legacy imports.
export { InboundService } from '@forge-core/channels';
export type { InboundResult } from '@forge-core/channels';
