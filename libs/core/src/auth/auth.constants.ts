/** Injection token for the resolved AuthTokenVerifier adapter */
export const AUTH_TOKEN_VERIFIER = 'AUTH_TOKEN_VERIFIER';

/** Injection token for the resolved AuthTokenIssuer adapter */
export const AUTH_TOKEN_ISSUER = 'AUTH_TOKEN_ISSUER';
