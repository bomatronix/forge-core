/**
 * Drizzle schema — single source of truth for all database tables.
 * Add tables here as features are built in spec-03+.
 */

// No tables yet — schema will grow as features are implemented.
// Export a placeholder to keep this a valid TS module.
// Example when adding a table:
// import { pgTable, uuid, text, timestamp } from 'drizzle-orm/pg-core';
// export const agents = pgTable('agents', { ... });
export {};
